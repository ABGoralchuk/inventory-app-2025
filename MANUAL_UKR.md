# 🧪 Інструкція з налаштування та запуску (Laboratory Inventory App)

Цей документ допоможе вам налаштувати Google Cloud, отримати API ключі, протестувати додаток локально та розгорнути його в Streamlit Cloud.

---

## 1. Налаштування Google Cloud (Google Sheets API)

Щоб додаток міг записувати дані в Google Таблицю, потрібно створити "Сервісний акаунт".

1.  Перейдіть до [Google Cloud Console](https://console.cloud.google.com/).
2.  Створіть новий проект (New Project) або виберіть існуючий.
3.  **Увімкніть API:**
    *   У меню зліва виберіть **APIs & Services** -> **Library**.
    *   Знайдіть та увімкніть **Google Sheets API**.
    *   Знайдіть та увімкніть **Google Drive API**.
4.  **Створіть Сервісний Акаунт:**
    *   Перейдіть до **APIs & Services** -> **Credentials**.
    *   Натисніть **Create Credentials** -> **Service Account**.
    *   Дайте ім'я акаунту (наприклад, `inventory-bot`) і натисніть **Create and Continue**.
    *   У ролях (Role) виберіть **Editor** (Basic -> Editor). Натисніть **Done**.
5.  **Отримайте JSON ключ:**
    *   У списку Service Accounts натисніть на email щойно створеного акаунту.
    *   Перейдіть на вкладку **Keys**.
    *   Натисніть **Add Key** -> **Create new key**.
    *   Виберіть **JSON** і натисніть **Create**. Файл автоматично завантажиться на ваш комп'ютер.
    *   **ВАЖЛИВО:** Збережіть цей файл, він знадобиться пізніше.
6.  **Надайте доступ до Таблиці:**
    *   Створіть нову Google Таблицю.
    *   Натисніть кнопку **Share** (Поділитися).
    *   Скопіюйте `client_email` з вашого JSON файлу (він виглядає як `inventory-bot@project-id.iam.gserviceaccount.com`).
    *   Вставте цей email у вікно Share і дайте права **Editor**.

---

## 2. Отримання ключа Gemini API

1.  Перейдіть до [Google AI Studio](https://aistudio.google.com/app/apikey).
2.  Натисніть **Create API key**.
3.  Скопіюйте отриманий ключ.

---

## 3. Локальний запуск (на вашому Mac)

1.  **Встановіть залежності:**
    Відкрийте термінал у папці проекту і запустіть:
    ```bash
    pip install -r requirements.txt
    ```

2.  **Налаштуйте секрети:**
    Створіть папку `.streamlit` та файл `secrets.toml` всередині:
    ```bash
    mkdir .streamlit
    touch .streamlit/secrets.toml
    ```
    Відкрийте `secrets.toml` і заповніть його за зразком `secrets.toml.example`. Вставте туди ваш `GEMINI_API_KEY` та вміст JSON файлу від Google Cloud (у секцію `[gcp_service_account]`).

3.  **Запустіть додаток:**
    ```bash
    streamlit run app.py
    ```
    Додаток відкриється у вашому браузері. Він автоматично підключиться до вашої таблиці (аркуш `Stock_In`).

---

## 4. Розгортання на Streamlit Cloud

Це дозволить вам користуватися додатком з телефону.

1.  **Завантажте код на GitHub:**
    *   Створіть новий репозиторій на GitHub.
    *   Завантажте туди файли: `app.py`, `ai_handler.py`, `sheets_handler.py`, `requirements.txt`.
    *   **НЕ ЗАВАНТАЖУЙТЕ** папку `.streamlit` або ваші ключі! Це небезпечно.

2.  **Створіть додаток на Streamlit Cloud:**
    *   Перейдіть на [share.streamlit.io](https://share.streamlit.io/).
    *   Увійдіть через GitHub.
    *   Натисніть **New app**.
    *   Виберіть ваш репозиторій, гілку (main) і файл `app.py`.

3.  **Налаштуйте секрети (Secrets):**
    *   Перед тим як натиснути Deploy, натисніть **Advanced settings**.
    *   Або після створення, в налаштуваннях додатку знайдіть розділ **Secrets**.
    *   Вам потрібно скопіювати вміст вашого локального файлу `.streamlit/secrets.toml` у це поле.
    *   Воно має виглядати приблизно так:

    ```toml
    GEMINI_API_KEY = "AIzaSy..."

    [gcp_service_account]
    type = "service_account"
    project_id = "..."
    ...
    ```

4.  Натисніть **Save** і **Deploy**.

Тепер ваш додаток доступний за посиланням! Відкрийте його на телефоні, надайте доступ до камери і скануйте етикетки. Дані автоматично потраплять на аркуш `Stock_In`.
